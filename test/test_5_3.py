# 5장 문제풀이 
# 3번 결과 예측하기 
print(all([1,2,abs(-3)-3]))
# 결과 false 이유는 요소중에 0 이 있기 때문이다. 

print(chr(ord('a')) == 'a')
# true 이유는 ord 는 문자를 유니코드로 변환해주는 함수이고 다시 chr 은 유니코드를 문자로 변환하는 함수이기 때문에
# 같은 문자라 트루이다. 